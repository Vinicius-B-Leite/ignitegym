export type ExerciseDTO = {
    created_at: string,
    demo: string,
    group: string,
    id: string,
    name: string,
    repetitions: string,
    series: number,
    thumb: string,
    updated_at: string
}